# AGENTS.md

本文件是 AI / Agent 进入 `Doc_Rule` 仓库后的**默认执行协议**。  
只要 Agent 能读取本仓库，就应主动按照本文件判断任务类型并执行，无需用户再次提供专门提示词。

## 0. 总目标

`Doc_Rule` 不是单纯的问题清单，而是一个面向技术方案、标书、合同及其他正式文件的**反例驱动写作规范库**。

Agent 使用本仓库时必须同时完成两个目标：

1. **沉淀经验**：从真实文档反馈中发现问题，去重后更新规则库。
2. **预防问题**：在新文档编写、改写和审查阶段主动应用规则，避免已知问题再次出现。

因此，本仓库既是“审查规则库”，也是“写作前置约束库”。

## 0.1 高阶质量判断优先于单条规则

Agent 不得只做“规则命中扫描”。

在任何正式文档任务中，先读取并理解：

- `principles/formal-deliverable-quality.md`
- `catalog/evaluation-criteria.yaml`

再读取：

- `catalog/rules.yaml`
- 与当前任务相关的具体规则文件

高阶评判标准用于判断一份文档在整体上是否真正像一份**成熟、克制、自然、一次成型、可以直接提交客户的正式成品**。

核心判断是：

> **客户应看到方案本身，而不是作者的写作过程、包装过程、重构过程、风险控制过程或自我证明过程。**

因此，Agent 的判断顺序必须是：

1. **整体成品判断**：文件身份、叙事重心、结构一致性、术语自然度、信息归属、工程表达、视觉克制等是否成立；
2. **已知规则检查**：判断是否命中现有规则；
3. **未知问题发现**：识别虽未命中规则，但违反高阶质量标准的问题；
4. **反馈沉淀**：仅在用户要求维护规则库时，对新问题执行去重并沉淀。

这意味着：  
**已有规则是下限，高阶质量标准是上位判断。**

---

## 1. 自动判断当前任务模式

Agent 读取本仓库后，先根据用户当前任务自动选择模式，不要等待用户说明“该怎么用这个仓库”。

### A. 写作模式（AUTHOR）

当用户要求：编写、起草、生成、改写、完善技术方案、标书、合同、实施方案、验收文档或其他正式文件时，进入写作模式。

必须执行：

1. 识别文档类型、接收方和正式程度。
2. 读取 `catalog/evaluation-criteria.yaml`，建立高阶写作约束。
3. 读取 `catalog/rules.yaml`，筛选适用规则。
4. 对高严重度规则读取对应 Markdown 全文。
5. **在写作前**把高阶标准和具体规则转换为写作约束，不得等文档写完后才检查。
6. 完成初稿后，先按高阶标准做整体成品检查，再按具体规则做独立自检并直接修正。
7. 最终只输出符合规则的成品，不向用户暴露内部自检过程，除非用户要求审查报告。

默认假设：除非用户明确说是内部草稿，否则文档将直接提交甲方、招标人、评审专家、合同相对方或最终使用部门。

详细流程见：`workflows/author-document.md`。

### B. 审查模式（REVIEW）

当用户要求：检查、审查、找问题、评估正式性、比较版本、指出不规范内容时，进入审查模式。

必须执行：

1. 先用高阶评判标准进行整体成品判断，不能一上来只扫已有规则。
2. 再读取规则索引并筛选适用规则。
3. 逐项定位违规证据，不能只做关键词扫描。
4. 区分：
   - 已有规则已覆盖；
   - 高阶标准发现但现有规则尚未覆盖；
   - 需要结合上下文判断；
   - 可保留。
5. 每个问题给出位置、问题说明和可执行修改建议。
6. 不因为规则存在就机械删除必要的合同边界、招标要求或安全禁止项。

详细流程见：`workflows/review-document.md`。

### C. 反馈沉淀模式（LEARN）

当满足以下任一情形时，进入反馈沉淀模式：

- 用户明确要求“记录、沉淀、加入仓库、形成规则、更新规则库”；
- 用户把真实文档或截图交给 Agent 分析，并且当前工作目标是维护 `Doc_Rule`；
- 用户指出某种“不希望再出现”的写作或格式问题，并要求以后 AI 避免；
- 用户要求根据新反例优化本仓库。

必须执行“**先抽象、再去重、后录入**”原则。不得为了增加规则数量而重复建规则。

候选问题的来源有两类：

1. 已有规则直接命中；
2. 高阶评判标准发现的、现有规则尚未覆盖的新问题。

详细流程见：`workflows/ingest-feedback.md`。

### D. 规则维护模式（MAINTAIN）

当用户要求调整分类、重构目录、合并规则、弃用规则、修改索引或提高 Agent 可用性时，进入维护模式。

维护时必须保证：

- `catalog/rules.yaml` 与规则文件一致；
- `catalog/evaluation-criteria.yaml` 与高阶原则文件一致；
- 规则 ID 稳定，不因标题改写随意变更；
- 已被新规则完全替代的规则使用 `deprecated` 或 `superseded` 状态，不直接删除历史语义；
- README、schema、workflow 与实际结构同步。

---

## 2. 反馈沉淀的去重判定

新反馈进入仓库前，必须先与现有规则做语义去重。至少检查：

- 标准问题描述是否相同；
- 问题发生机制是否相同；
- 对正式文件造成的风险是否相同；
- 推荐修正原则是否相同；
- 新问题是否只是已有规则的一个新表现形式。

候选反馈必须归入以下四类之一：

### DUPLICATE — 完全重复

新反馈与现有规则本质一致，且没有增加新的典型表现。

处理：**不新增规则，不修改仓库。**

### EXTENSION — 已有规则的新表现

问题机制和修正原则相同，但新反馈提供了新的典型场景、检测线索、文档类型或更好的示例。

处理：**更新已有规则**，优先补充：
- 新反例；
- 新推荐表达；
- detection hints；
- applies_to；
- 判定标准。

不得新建同义规则。

### NEW — 独立的新问题

只有当新反馈具有独立的问题机制，并且无法在不扭曲原规则语义的情况下归入现有规则时，才新建规则。

处理：
1. 选择合适规则族；
2. 分配新规则 ID；
3. 使用 `rules/_template.md` 创建规则；
4. 更新 `catalog/rules.yaml`；
5. 必要时更新 taxonomy / schema / README。

### CONFLICT — 与现有规则冲突

新反馈与已有规则方向冲突，或适用场景存在明显矛盾。

处理：**不得直接覆盖原规则。**  
应标记冲突点，并请求人工确认；若可通过“适用范围/例外”化解，则更新规则边界。

---

## 3. “重复”的判断标准

判断重复时，不按字面词语判断，而按**底层失败模式**判断。

例如：

- “客户已明确……”；
- “如客户需要……”；
- “客户提供……”；

虽然句子不同，但底层问题都是“正式项目文件使用销售/内部沟通视角称呼文件接收方”，应归入同一规则，而不是拆成三条。

再例如：

- 页眉出现 `V0.2`；
- 封面出现“最终修改稿”；
- 修订记录暴露 V0.1～V0.5 内部演进；

底层问题都是“对外文件暴露内部迭代文控信息”，原则上归入同一规则。

---

## 4. 写作阶段必须主动应用规则

Agent 在写新文档时不得只把本仓库当“完稿后的检查清单”。

每条适用规则应转换成两类约束：

### Preventive Constraint — 编写前约束

例如：

- 不生成内部版本号；
- 不使用未定义项目简称；
- 不在正文使用文档自述式元话语；
- 不输出内部工作编号；
- 责任边界优先写成职责分工；
- 统一使用正式主体称谓；
- 保持单一文档身份；
- 让解决方案而不是风险控制成为主叙事；
- 不为“显得专业”创造多余术语体系；
- 不保留旧稿迁移或重构痕迹；
- 保证信息有唯一主位置；
- 工程复杂度与图、表、文字表达相匹配。

### Verification Check — 完稿后验证

例如：

- 搜索是否仍有 `V0.`、`Draft`、内部简称；
- 检查图注是否混入内部素材来源；
- 检查表格是否出现内部字母数字编码；
- 检查“不负责、不包含、仅用于、不作为”等否定表达是否过度；
- 检查主体称谓是否全文一致；
- 检查文件身份是否混杂；
- 检查是否存在旧稿、原方案、重新映射等迁移痕迹；
- 检查同一信息是否在多处重复展开；
- 检查视觉包装是否喧宾夺主；
- 检查复杂工程关系是否缺少必要图示。

**写作 = 高阶判断 + 预防 + 验证，而不是“先犯错，再修错”。**

---

## 5. 规则的标准结构

每条规则至少包含：

- 标准问题描述；
- 为什么是问题；
- 判定标准；
- 至少 2 个反例；
- 至少 2 个推荐表达或处理方式；
- 检查方法；
- 必要例外。

推荐额外包含：

- 典型触发词 / detection hints；
- 适用文档类型；
- 与其他规则的关系；
- 编写阶段的 preventive guidance。

示例应尽量去标识化，不写真实客户名称、敏感项目编号、未公开业务数据或其他不应公开的信息。

---

## 6. 审查时的输出要求

审查结果建议采用：

```text
[DR-FRM-007] 责任边界采用正向职责表达
位置：第 X 页 / 第 X 节
原文：……
问题：……
建议：……
```

对于尚未形成具体规则、但由高阶标准发现的问题，可使用：

```text
[Q-04] 结构原生一致
位置：第 X 页 / 第 X 节
表现：……
问题：……
建议：……
```

或表格：

| 评判/规则编号 | 位置 | 原文/表现 | 问题说明 | 建议修改 |
|---|---|---|---|---|

但如果用户只要求最终修订稿，应直接修正文档，不必额外输出长篇审查过程。

---

## 7. 不能机械执行的事项

以下情况必须结合上下文判断：

- 合同明确排除项；
- 招标文件逐条响应要求；
- 法规、国家标准、行业标准的强制性措辞；
- 安全禁止项，如“不得旁路安全回路”；
- 甲方正式文控要求的版次、编号、修订记录；
- 正式引用的第三方标准、数据或图表来源。

规则的目标是消除**内部感、草稿感、AI 感和不专业表达**，不是删除必要的法律、技术和安全边界。

---

## 8. 最小读取策略

为避免规则库扩大后每次加载全部文件，Agent 应：

1. 先读取 `catalog/evaluation-criteria.yaml`；
2. 再读取 `catalog/rules.yaml`；
3. 根据 `applies_to`、`category`、`severity`、`detection_hints` 筛选；
4. 只加载高相关规则全文；
5. 若是反馈沉淀模式，再额外加载语义最相近的候选规则用于去重。

若任务仍有歧义，优先遵循“正式成品文档”的更严格标准。